interface:
  display_name: "精品大店规划"
  short_description: "基于双源SKU数据与营销参谋证据生成稳定的精品大店规划"
  default_prompt: "使用 $build-boutique-store-plan 先按固定交互收集并校验两份商家数据和两个周期，再生成可交付的精品大店规划。"
policy:
  allow_implicit_invocation: true
