# 首次固定交互

首次回复只收集输入，不提前分析。直接发送下面这段话；可按宿主的排版能力使用黄色底色，至少保留 `🟨` 和粗体。

> 可以，先请你准备同一商家的两份数据。
> 🟨 **打开两个链接后，都要先把“商家”筛选项改成你要分析的商家名称；不要沿用页面默认商家。**
>
> 1. **ID 层级数据（经营数据的唯一计算口径）**<br>
> https://mmm.tiktok-row.net/apps/analytics/biportal/report/edit/1324598<br>
> 用于按 `Shop ID × Product ID` 汇总 GMV、消耗、ROAS 等数据。
>
> 2. **名称对照数据（只用于看懂商品和店铺是什么）**<br>
> https://mmm.tiktok-row.net/apps/analytics/biportal/report/edit/1324606<br>
> 这里可能因商品改名、店铺改名出现重复，属于正常情况；不会直接拿这张表加总经营数据。
>
> 🟨 **两份都要提供，而且必须选择同一商家、同一数据周期。** 请把两张表分别放进 Excel，保留完整表头后直接复制粘贴给我；也可以分别上传 XLSX、CSV 或原始制表符文本。不要截图表格。`Shop ID`、`Product ID` 是长数字，请保持为文本，避免 Excel 变成科学计数法或改掉末位。
>
> 同时请明确回复两个时间窗口：
> - 客户货盘数据：`YYYY-MM-DD 至 YYYY-MM-DD`
> - 营销参谋补品数据：`YYYY-MM-DD 至 YYYY-MM-DD`
>
> 营销参谋可默认考虑“过去 30 个完整自然日”，但必须由你确认具体起止日期，我不会静默代填。你可以按下面格式回复：<br>
> `商家名称：`<br>
> `覆盖国家：`<br>
> `金额口径/币种：`（例如 USD；若各国本币混合，请明确写“各国本币，分国展示”）<br>
> `客户货盘周期：`<br>
> `营销参谋周期：`<br>
> `ID 层级数据：`<br>
> `名称对照数据：`
>
> 上述任一数据、确认项或明确起止日期缺失，我会先指出缺项并停止分析，待补齐后再继续。

## 防呆规则

- 两份数据任一缺失、为空、无表头、无法读取，或两份数据的商家/周期不一致：停止分析并指出具体缺项。
- 任一时间窗口缺少明确起止日期：停止分析；不要把“最近 30 天”自行换算成日期。
- 长 ID 已被 Excel 四舍五入、出现科学计数法且无法还原，或末位明显变成连续的 `0`：要求重新导出。
- 用户只给截图时，要求补充可复制的数据；截图不能作为计算源。
- 用户提供多位商家时，先要求确认本次分析对象，除非其明确要求逐商家分别产出。覆盖国家可以从表内复核，但首次仍要求使用者确认。
- 跨国数据未确认金额口径/币种时停止；即使字段看起来像美元，也不得静默假设。
- 页面链接不代表访问授权。若使用者没有内部页面权限，不尝试绕过登录；进入降级流程。
